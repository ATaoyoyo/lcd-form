export const typeKeyMap = {
  input: "text",
  inputNumber: "number",
  datePicker: "date",
  table: "formTable",
};

export const valueKeyMap = {
  table: [],
  inputNumber: null,
};

// 'text', 'number', 'select', 'date', 'radio'
